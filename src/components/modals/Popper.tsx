/**
 * Popper in this kit is the same primitive as PopoverContent,
 * kept for VKUI-like naming.
 */
export { PopoverRoot as PopperRoot, PopoverTrigger as PopperTrigger, PopoverClose as PopperClose, PopoverContent as PopperContent } from "./Popover";
